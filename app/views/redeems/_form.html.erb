<%= form_for(@redeem) do |f| %>
  <% if @redeem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@redeem.errors.count, "error") %> prohibited this redeem from being saved:</h2>

      <ul>
      <% @redeem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :share_id %><br>
    <%= f.number_field :share_id %>
  </div>
  <div class="field">
    <%= f.label :device_id %><br>
    <%= f.text_field :device_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
